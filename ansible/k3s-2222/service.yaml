apiVersion: v1
kind: Service
metadata:
  name: k3s-2222-service  # サービス名
spec:
  selector:
    app: k3s-2222-app    # 連携するPodのラベルを指定
  ports:
    - protocol: TCP
      port: 2222       # サービスが受け付けるポート
      targetPort: 2222 # Pod側で動いているアプリのポート
  type: ClusterIP      # クラスター内部用（外部公開ならNodePort等）
